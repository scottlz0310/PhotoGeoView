#!/usr/bin/env bash
# ログファイルをリアルタイム監視

LOG_FILE="logs/photogeoview.log"

echo "👀 ログファイルを監視中: $LOG_FILE"
echo "   (Ctrl+C で停止)"
echo "================================"

# ログファイルが存在しない場合は作成
touch "$LOG_FILE"

# tail -f でリアルタイム表示
tail -f "$LOG_FILE"
