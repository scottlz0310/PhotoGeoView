name: Build

on:
  push:
    branches: [ main ]
    paths:
      - 'package.json'
      - 'electron.vite.config.ts'
      - 'src/**'
      - '.github/workflows/build.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'package.json'
      - 'electron.vite.config.ts'
      - 'src/**'
      - '.github/workflows/build.yml'

permissions:
  contents: read

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Unit Tests
        run: pnpm test

      - name: Build App
        run: pnpm build

      - name: Package App
        # Skip signing for now on macOS/Windows to avoid errors without certs
        # On macOS, identity=null disables code signing
        run: |
          if [ "$RUNNER_OS" == "Windows" ]; then
            pnpm package --win --config.mac.identity=null
          elif [ "$RUNNER_OS" == "macOS" ]; then
            pnpm package --mac --config.mac.identity=null
          else
            pnpm package --linux --config.mac.identity=null
          fi
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEBUG: electron-builder

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: PhotoGeoView-${{ matrix.os }}
          path: |
            dist/*.exe
            dist/*.dmg
            dist/*.AppImage
            dist/*.snap
            dist/*.deb
            dist/*.rpm
            dist/*.zip
          if-no-files-found: warn
